# Plan ujednolicenia nazewnictwa — małe kroki

## Cel

Ustalić i zastosować spójną konwencję nazewnictwa plików/folderów w projekcie, wykonując zmiany w małych,
bezpiecznych krokach — każdy krok to osobny commit, łatwy do cofnięcia.

## Proponowana konwencja

| Typ | Konwencja | Przykład |
|-----|-----------|----------|
| JS/TS pliki | `camelCase` | `wellManager.ts`, `pvMarketplace.ts` |
| CSS pliki | `lowercase` | `style.css`, `studnie.css` (obecnie OK) |
| HTML pliki | `lowercase` | `index.html`, `kartaBudowy.html` |
| Katalogi | `lowercase` | `src/routes/`, `public/js/sales/` |
| Testy | `*.test.ts` z camelCase bazą | `pvMarketplace.test.ts` |
| Python | `snake_case` (standard PEP8) | `rule_engine/` (obecnie OK) |

## Zasada testowania

Po każdej zmianie nazwy pliku:
1. Uruchomić `npm test` aby upewnić się, że testy nadal przechodzą
2. Jeśli plik ma już test — zmienić nazwę testu zgodnie z nową nazwą pliku
3. Jeśli plik NIE ma testów — dodać jeden podstawowy test (smoke test: import działa, zwraca oczekiwany typ)
4. Po uruchomieniu testów sprawdzić `npm run lint`

## Krok 1 — Konwencja w AGENTS.md

Dodać sekcję "Nazewnictwo plików i folderów" do `.kilo/AGENTS.md` z powyższą tabelą.

**Pliki:** `.kilo/AGENTS.md`
**Commit:** `docs: dodanie konwencji nazewnictwa do AGENTS.md`

## Krok 2 — `src/routes/pv_marketplace.ts` → `pvMarketplace.ts`

Zmienić nazwę pliku + poprawić importy.

**Zmiana:** `src/routes/pv_marketplace.ts` → `src/routes/pvMarketplace.ts`
**Importy do poprawy:** `src/routes/index.ts` (lub inny aggregator)
**Test:** Jeśli istnieje `tests/pvMarketplace.test.ts` → zmienić nazwę; jeśli nie → dodać smoke test
**Weryfikacja:** `npm test`, `npm run lint`
**Commit:** `refactor: ujednolicenie nazwy pv_marketplace.ts → pvMarketplace.ts (camelCase)`

## Krok 3 — `public/js/sales/` — ujednolicenie 2 plików

| Obecnie | Po zmianie |
|---------|-----------|
| `PV_MarketplaceManager.js` | `pvMarketplaceManager.js` |
| `pv_sales_ui.js` | `pvSalesUi.js` |
| `kartotekaInit.js` | bez zmian (już camelCase) |

**Importy do poprawy:** znaleźć referencje w `public/app.html`, `public/kartoteka.html` i innych HTML
**Testy:** dodać smoke testy dla `pvSalesUi` i `pvMarketplaceManager` (sprawdzić czy ładują się w JS)
**Weryfikacja:** `npm test`, ręczne sprawdzenie w przeglądarce (jeśli frontend działa)
**Commit:** `refactor: ujednolicenie nazw w public/js/sales/ (camelCase)`

## Krok 4 — `public/js/shared/icons-slim.js` → `iconsSlim.js`

**Importy do poprawy:** `<script>` w `public/*.html` (app.html, studnie.html, rury.html, etc.)
**Testy:** dodać smoke test: czy `iconsSlim` jest obiektem, czy eksportuje ikony
**Weryfikacja:** `npm test`
**Commit:** `refactor: ujednolicenie icons-slim.js → iconsSlim.js (camelCase)`

## Krok 5 — `src/services/antygrawity/` → `src/services/antiGravity/`

**Zmiana:** katalog + importy wewnątrz
**Importy do poprawy:** wszystkie pliki importujące z `src/services/antygrawity/`
**Testy:** dodać smoke test dla `antiGravity/index.ts` (czy serwis się inicjalizuje)
**Weryfikacja:** `npm test`, `npm run lint`
**Commit:** `refactor: antygrawity/ → antiGravity/ (angielska nazwa katalogu)`

## Krok 6 — `public/templates/` snake_case → lowercase

| Obecnie | Po zmianie |
|---------|-----------|
| `oferta_studnie.html` | `ofertaStudnie.html` |
| `oferta_studnie_template.docx` | `ofertaStudnie.docx` |
| `karta_budowy.html` | `kartaBudowy.html` |
| `etykieta.html` | bez zmian (OK) |
| `zlecenie.html` | bez zmian (OK) |

**Importy do poprawy:** `src/services/docx/studnie/sections.ts`, `src/services/docx/ruryDocx.ts`,
`public/studnie.html`, `public/app.html`
**Testy:** dodać smoke test dla każdego szablonu (czy plik istnieje, czy referencja jest poprawna)
**Weryfikacja:** `npm test`
**Commit:** `refactor: ujednolicenie nazw w public/templates/ (camelCase)`

## Krok 7 — `public/js/app_studnie.js` → `appStudnie.js`

**Importy do poprawy:** `<script src>` w `public/studnie.html`
**Testy:** dodać smoke test: czy `appStudnie` inicjalizuje moduł studni
**Weryfikacja:** `npm test`
**Commit:** `refactor: app_studnie.js → appStudnie.js (camelCase)`

## Krok 8 — `scripts/` — ujednolicenie aktywne

Zmienić tylko aktywne skrypty (nie `archive/`):

| Obecnie | Po zmianie |
|---------|-----------|
| `check-db.ts` | `checkDb.ts` |
| `create-docx-template.ts` | `createDocxTemplate.ts` |
| `download_fonts.js` | `downloadFonts.js` |
| `migrate-emojis.js` | `migrateEmojis.js` |
| `temp_rules.js` | `tempRules.js` |

**Testy:** sprawdzić czy skrypty są używane w `package.json` scripts — jeśli tak, zaktualizować
**Weryfikacja:** `npm test` (skrypty nie są testowane, ale build musi przechodzić)
**Commit:** `refactor: ujednolicenie nazw w scripts/ (camelCase)`

## Krok 9 — test `sqlInjection-e2e.test.ts` → `sqlInjectionE2e.test.ts`

**Zmiana:** tylko nazwa pliku
**Testy:** uruchomić `npm test` — test sam siebie weryfikuje
**Weryfikacja:** `npm test`
**Commit:** `refactor: sqlInjection-e2e.test.ts → sqlInjectionE2e.test.ts`

## Krok 10 — `scripts/archive/` — przegląd i sprzątanie

Przejrzeć 20 plików w `scripts/archive/`:
- Usunąć jednorazowe skrypty migracyjne (np. `update_*`, `clean_fields.js`, `fix.js`)
- Zostawić tylko przydatne narzędzia (np. `check-raw-db.ts`, `clear-db.ts`)
- Dodać do planu na osobnego commita

**Weryfikacja:** `npm test` (archiwum nie jest importowane)
**Commit:** `chore: usuniecie niepotrzebnych skryptow migracyjnych z scripts/archive/`

## Krok 11 (opcjonalny) — `well_configurator_backend/` → `well-configurator-backend/`

Zmiana snake_case na kebab-case w nazwie katalogu. **Uwaga:** wymaga aktualizacji
wszystkich ścieżek: `start_all.bat`, docker, skrypty startowe, konfiguracje.
Wysoki koszt zmiany — do dyskusji.

## Krok 12 (opcjonalny) — `ECC/`, `graphify-out/` → lowercase

Zmiana nazwy katalogów `ECC/` → `ecc/`, `graphify-out/` → `graphify-out/` (już OK).
Wymaga aktualizacji submodułu ECC. Średni koszt.

## Uwagi

- Każdy krok to osobny commit → łatwe cofnięcie w razie problemów
- Po każdym kroku: `npm test` + `npm run lint`
- Po kroku 10 dodać do `.gitignore` stare nazwy (opcjonalnie), żeby nikt nie commitował pod starą nazwą
