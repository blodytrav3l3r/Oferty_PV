# Plan: Dodanie pola "Data ważności oferty" w zakładce Oferta (studnie)

## Cel

Dodać pole `#offer-tab-validity` w zakładce "Oferta" obok "Warunki płatności", które automatycznie przepisuje wartość z kroku 1 (`#offer-validity`). Zaktualizować zapis/odczyt/czyszczenie oraz wydruki, aby korzystały z tego pola.

---

## Obecny stan

| Element | Status |
|---------|--------|
| `#offer-validity` w kroku 1 (wizard) | ✅ istnieje, default "7 dni" |
| `#offer-tab-payment-terms` w zakładce Oferta | ✅ istnieje, sync z kroku 1 |
| `#offer-tab-validity` w zakładce Oferta | ❌ **brak** |
| Save: `paymentTerms` czyta z tab → fallback wizard | ✅ (linia 1431-1434) |
| Save: `validity` czyta tylko z wizard | ⚠️ (linia 1435) |
| Load: `paymentTerms` → oba pola | ✅ (linie 1985-1991) |
| Load: `validity` → tylko wizard | ⚠️ (linie 1993-1994) |
| Clear: `paymentTerms` → oba pola | ✅ (linie 1751-1756) |
| Clear: `validity` → tylko wizard | ⚠️ (linie 1757-1758) |
| Sync wizard→tab dla `paymentTerms` | ✅ (linie 27-34) |
| Sync wizard→tab dla `validity` | ❌ **brak** |
| Wydruk: `paymentTerms` z wizarda | ✅ ale powinien czytać z tab |
| Wydruk: `validity` z wizarda | ⚠️ ale powinien czytać z tab |

---

## Etapy zmian (7 małych etapów)

### Etap 1: HTML — dodanie pola w zakładce Oferta

**Plik:** `public/studnie.html` ~linia 3324-3338

Dodać input `#offer-tab-validity` w karcie "Warunki płatności" (lub obok niej). Wzór: grid 3fr 1fr (jak w wizard), gdzie lewa strona to textarea payment terms, prawa strona to input validity.

```html
<!-- WARUNKI PŁATNOŚCI + DATA WAŻNOŚCI -->
<div class="card" style="margin-bottom: 1rem; padding: 0.8rem;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.5rem;">
        <label style="font-size: 0.85rem; color: var(--text-primary); font-weight: 700;">
            <i data-lucide="credit-card"></i> Warunki płatności
        </label>
    </div>
    <div style="display: grid; grid-template-columns: 3fr 1fr; gap: 0.8rem;">
        <textarea id="offer-tab-payment-terms" class="wizard-form-input" rows="2" placeholder="Domyślnie przepisywane z kroku 1..." style="resize: vertical; width: 100%; font-size: 0.75rem;"></textarea>
        <div>
            <label style="font-size: 0.7rem; color: var(--text-secondary); font-weight: 600; display: block; margin-bottom: 0.25rem;">Data ważności</label>
            <input id="offer-tab-validity" class="wizard-form-input" style="width: 100%; font-size: 0.75rem;" />
        </div>
    </div>
</div>
```

### Etap 2: JS — sync wizard → tab dla validity

**Plik:** `public/js/studnie/offerManager.js` ~linia 27-34

Dodać sync dla `#offer-validity` → `#offer-tab-validity` (gdy pole w zakładce jest puste), obok istniejącego synca payment terms:

```javascript
// Sync validity if empty
const validityTabField = document.getElementById('offer-tab-validity');
if (validityTabField && !validityTabField.value.trim()) {
    const originalValidity = document.getElementById('offer-validity');
    if (originalValidity) {
        validityTabField.value = originalValidity.value;
    }
}
```

### Etap 3: JS — save czyta z tab → fallback wizard

**Plik:** `public/js/studnie/offerManager.js` ~linia 1435

Zmienić z:
```javascript
const validity = document.getElementById('offer-validity')?.value.trim() || '7 dni';
```
na:
```javascript
const validity =
    document.getElementById('offer-tab-validity')?.value.trim() ||
    document.getElementById('offer-validity')?.value.trim() ||
    '7 dni';
```

### Etap 4: JS — load wypełnia pole w zakładce

**Plik:** `public/js/studnie/offerManager.js` ~linia 1993-1994

Dodać ustawienie `#offer-tab-validity` (wzór: payment terms lines 1990-1991):

```javascript
if (document.getElementById('offer-validity'))
    document.getElementById('offer-validity').value = normalized.validity || '7 dni';

const tabValidity = document.getElementById('offer-tab-validity');
if (tabValidity) tabValidity.value = normalized.validity || '7 dni';
```

### Etap 5: JS — clear czyści pole w zakładce

**Plik:** `public/js/studnie/offerManager.js` ~linia 1757-1758

Dodać reset `#offer-tab-validity` (wzór: payment terms lines 1755-1756):

```javascript
if (document.getElementById('offer-validity'))
    document.getElementById('offer-validity').value = '7 dni';
const tabValidity = document.getElementById('offer-tab-validity');
if (tabValidity) tabValidity.value = '7 dni';
```

### Etap 6: Wydruk — czytaj validity z tab → fallback wizard

**Plik:** `public/js/studnie/offerPrintManager.js` ~linia 261

Zmienić z:
```javascript
const validity = document.getElementById('offer-validity')?.value?.trim() || '';
```
na:
```javascript
const validity = document.getElementById('offer-tab-validity')?.value?.trim() || document.getElementById('offer-validity')?.value?.trim() || '';
```

Oraz analogicznie dla `paymentTerms` (linia 260):
```javascript
const paymentTerms = document.getElementById('offer-tab-payment-terms')?.value?.trim() || document.getElementById('offer-payment-terms')?.value?.trim() || '';
```

### Etap 7: normalizacja — obsługa `validity` i `paymentTerms` przy wczytywaniu oferty

**Plik:** `public/js/studnie/offerManager.js` ~linia 1206-1257

Sprawdzić czy `validity` i `paymentTerms` są w liście pól w `normalizeOfferData()`. Jeśli tak — OK, jeśli nie — dodać. Obecnie już są na liniach 1224-1225, więc to tylko weryfikacja.

---

## Czego NIE zmieniamy

- **Rury (`rury.html`, `rury/offerCrud.js`)** — nie mają oddzielnej zakładki "Oferta", formularz jest jednostronicowy. `#offer-validity` już działa poprawnie.
- **Builder DOCX (`src/services/docx/studnie/builder.ts`)** — czyta `offerData.validity` z zapisanego dokumentu, który będzie pochodził z tab field.
- **pdfGenerator.ts** — czyta `data.validity` z zapisanego dokumentu (linie 471, 579), nie wymaga zmian.
- **ofertaStudnie.html (template)** — już wyświetla `{{DATA_WAZNOSCI}}`.
- **backend API** — nie wymaga zmian, `data` JSON zawiera `validity` i `paymentTerms`.

---

## Weryfikacja

1. Otworzyć zakładkę "Oferta" → sprawdzić czy pole "Data ważności" istnieje obok "Warunki płatności"
2. Zmienić wartość w kroku 1 → przejść do zakładki Oferta → sprawdzić czy pole w zakładce jest puste (sync tylko przy pustym polu)
3. Wypełnić pole w zakładce → zapisać ofertę → odświeżyć → wczytać → sprawdzić czy wartość się zachowała
4. Wyczyścić formularz ("Nowa oferta") → sprawdzić czy oba pola mają "7 dni"
5. Wydruk → sprawdzić czy data ważności na wydruku odpowiada polu z zakładki
6. `npm test` — wszystkie testy muszą przejść
