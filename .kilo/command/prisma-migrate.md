---
description: Wykonaj migrację Prisma (z opisem)
---
Wykonaj migrację bazy danych: `npm run prisma:migrate`

Jeśli podano opis ($1), użyj go: `npx prisma migrate dev --name "$1"`

Po migracji wygeneruj klienta: `npm run prisma:generate`
