npm run version:check 2>&1 || { echo "❌ Wersja niespójna — przerwano push"; exit 1; }
npm run encoding:check 2>&1 || { echo "❌ Błąd kodowania (mojibake/UTF-8) — uruchom npm run encoding:fix i dodaj poprawki"; exit 1; }
npm run appname:check 2>&1 || { echo "❌ Nazwa aplikacji odbiega od S.O.K. — uruchom npm run appname:check i napraw wzorce"; exit 1; }
npm run lint 2>&1 || { echo "❌ Lint backend — uruchom npm run lint i napraw błędy"; exit 1; }
npm run lint:frontend 2>&1 || { echo "❌ Lint frontend — uruchom npm run lint:frontend i napraw błędy"; exit 1; }
npm run typecheck 2>&1 || { echo "❌ Typecheck backend"; exit 1; }
npm run typecheck:frontend 2>&1 || { echo "❌ Typecheck frontend"; exit 1; }
npm run test:quick:lite 2>&1 || { echo "❌ Testy lite — uruchom npm run test:quick:lite"; exit 1; }
npx jest --selectProjects git-safety --runInBand --no-coverage 2>&1 || { echo "❌ Testy git-safety — uruchom npx jest --selectProjects git-safety --runInBand"; exit 1; }
