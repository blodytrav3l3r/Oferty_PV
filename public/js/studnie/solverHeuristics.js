// @ts-check
/**
 * solverHeuristics.js — Funkcje heurystyczne solvera studni
 *
 * Funkcje zostaly przeniesione do solverAutoSelect.js jako zagniezdzone
 * w runJsAutoSelection(). Ten plik pozostaje jako rezerwacja nazwy.
 *
 * Wszystkie rzeczywiste implementacje:
 *   parseHoleClearance, buildTopConfig, fillKregiGreedy, fillKregiDP,
 *   findBestAvrFill, checkConflicts — znajduja sie w solverAutoSelect.js
 *   wewnatrz funkcji runJsAutoSelection() (closure, nie global scope).
 */
