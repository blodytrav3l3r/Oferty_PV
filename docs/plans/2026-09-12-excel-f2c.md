# Plan F2c — odroczenie listy spod modala (WYKONANY 12.09.2026)

Data: 2026-09-12
Status: WYKONANY (STOP-CONTROL, bez commita — osobna decyzja)
Poprzednik: `2026-09-12-excel-f2.md`
Wariant: A+B (guard + odroczenie). Bez commita.

## Diagnoza (bench-bazowy, in-page, fixture 1200)

Koszt commita edycji MANUAL (mediany): `updateSummary` 154 ms (w tym
wewnętrzny `renderWellsList` ~108) + jawny `renderWellsList` 108 ms +
diagram 0,1 + kody h3 1,5 + recalc kolejki 0. Czyli lista 2× (~216 ms
samego list-renderingu) na każdy commit — a leży pod overlayem modala.

Ścieżka AUTO (solver) ma własną kaskadę (`autoSelectComponents` + preview)
i jest poza tym planem — debounced-path dotyczy edycji MANUAL.

## Zmiany (3 pliki + flaga, ten sam output)

1. `excelState.js`: `let _excelListStale = false` (stawia debounced, konsumuje
   close).
2. `excelPolling.js` (`_excelDebouncedRefresh`): guard `_renderingWellsList`
   wokół `updateSummary()` — tłumi wewnętrzny render #1 (wzór z
   `_excelSyncMainPreview`); jawny `renderWellsList` wykonywany tylko gdy
   brak overlayu, inaczej `_excelListStale = true`.
3. `excelModal.js` (`_excelCloseOverlay`): jeśli stale → jeden
   `renderWellsList`. Wszystkie ścieżki close i tak kończą `refreshAll`
   (idempotentny) — twarda gwarancja świeżej listy.
4. `excelAutoSelect.js` (`_excelToggleWellAutoMode`): ten sam guard
   (identyczny podwójny render przy przełączniku A/M).

Audyt close: `closeExcelTableModal` (X/Esc) → overlay + `refreshAll`;
`excelSaveAll` → `refreshAll` + overlay; obie świeże. Precedens: flaga
`__excelBulkDepth` (quiet-bulk) robi to samo dla operacji masowych.

## Gate F2c (throwaway probe, PASS)

Edycja MANUAL na fixture: rendery listy w trakcie 2→0, flaga stale=true,
kafelki żywe (cena zmieniona), po close dokładnie 1 render, lista pełna.
Czas commita (w tym 1500 ms wait): 2766→1788 (real ~1260→~290).

## Bench open (ostrzeżenie metodologiczne)

Przebieg `excelOpenPerf` po F2c wypadł podwyższony (cold 799 / warm 651)
przy AKTYWNYM obciążeniu desktopu (gra + komunikatory na tym samym hoście;
CPU-User). Nie jest to regresja F2c (zmiana nie tyka ścieżki open; mikro-
i gate-proby back-to-back pokazują zysk), tylko zaszumione środowisko.
Liczb z tego przebiegu NIE używać do porównań — plik
`baseline-...-f1.json` został nim nadpisany (cold 799 / warm 651 pod
obciążeniem). Dobre liczby F2 (cold 589 / warm 551 / open-total 383)
pozostają w `2026-09-12-excel-f2.md` i w historii gita pliku
(`git show HEAD:docs/plans/baseline-excel-open-1200-f1.json`).
Bench powtórzyć na spokojnym hoście przed ewentualnym RFC.

## Gates

`tests/studnie` 87/1267 PASS, `typecheck:frontend` PASS, `lint:frontend`
0 errors, `test:parity` PARITY OK, `test:alignment` PASS 6/6, visual 0px,
DOM-golden OK + sensitivity, scroll-kontrakt OK, F2c-gate PASS.
Pełny `test:quick` + `test:axe`: przed commitem (jak F2).
