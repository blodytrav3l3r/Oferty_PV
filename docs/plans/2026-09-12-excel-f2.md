# Plan F2 — dokończenie contractu (WYKONANY 12.09.2026)

Data: 2026-09-12
Status: WYKONANY (STOP-CONTROL, bez commita — osobna decyzja)
Poprzednicy: `2026-09-12-excel-open-perf-1000.md` (F1),
`2026-09-12-excel-gates-f1a.md` (gates)
Decyzje: contract skorygowany (open-total ≤360 zamiast ×0,5); F2b z bramką
mutation testu. Bez commita.

## F2.0 — audyt readers/writers (bez kodu, bramka F2b)

- Writers: wszystkie mutacje Excel wołają `_excelInvalidateFilteredIndexes`;
  luki panelu głównego kryje polling-diff (ustalone w F1).
- Readers: `refreshAllWellErrors()` liczy WSZYSTKIE wells i wołają go
  `renderWellsList` (każdy render), `offerRendering:22`, `wellVirtual:452`
  — oferta czyta status zawsze po recompute. Wniosek: lazy-errors bezpieczne
  przy liczeniu wchodzącego taba w `excelSwitchTab`.
- `config` (ilości/gaskets) czytają globalni odbiorcy bez guardów
  (`updateSummary`, oferta, eksport) → **lazy-prep ODRZUCONE ostatecznie**
  (41 ms nie warte ryzyka rozjazdu totali w oknie debounce 800 ms).

## F2a — hoisty/memo (ten sam output)

1. Hoist `przProducts`/`categories`/`availDns`-memo/`wlazCol` per render
   (`excelTableBody.js`) — DN z pierwszej studni taba, kopia kategorii per
   slot, `_przById` z semantyką first-match jak `find`.
2. Memo `_excelGetWellProdCode` per `well.id|ct|height|targetDn` + sygnatura
   `config|availKey|products.length` (klasa staleness jak `__availCache`);
   ceny (`Price`) celowo bez memo (live `getItemAssessedPrice`).
   Usunięty dubel `_excelUpdateHeaderProdCodes()` w `excelSwitchTab`
   (render i tak nim się kończy).
3. `_excelProdFp()` — fingerprint cennika raz per tożsamość tablicy
   (WeakMap) + walidacja długością; poprawny przy setterze i
   direct-assignment bypass.
4. Po drodze znaleziony własny błąd: brak nawiasów w sygnaturze
   (`||` vs `+` gubił quantity) — naprawiony przed bench.

Kontrola A/B (stash): wall-mediany zaszumione (σ~80, graphify-rebuild w tle
przy pierwszych próbach); rozstrzygnął mikro-bench in-page 12×:
`_excelRenderTable` 356→290 ms (−19%), `_excelRenderTbody` 100→87 ms.

## F2b — per-tab lazy errors (z bramką)

5. Open liczy błędy tylko aktywnego taba (`_excelRecalcTabWellErrors`,
   `excelPolling.js`); `excelSwitchTab` dolicza wchodzący tab PRZED renderem;
   banner `renderWellConfigErrors(currentWell)` zachowany.
6. Bramka mutation test (throwaway probe, PASS): mutacja `rzednaDna` w DN1500
   z panelu → po open status nietknięty (lazy proof `OK`) → po switchu
   `ERROR` + klasa `excel-row-error` + tooltip → po naprawie i
   switchu tam/z powrotem `OK` bez klasy.

## Wynik (ten sam fixture 1200, mediana 5+5, `baseline-...-f1.json`)

| Metryka     |    F0 |      F1 |                           F2 | Cel F2                      |
| ----------- | ----: | ------: | ---------------------------: | --------------------------- |
| open-total  |   548 |     454 |                          383 | ≤360 (~, wariancja ±50)     |
| cold wall   |   763 |     659 |                          589 | info                        |
| warm wall   |     — |     642 |                          551 | info                        |
| open-errors |    43 |      43 |                         10,7 | —                           |
| tbody       |    49 |      41 |                           36 | —                           |
| switch tab  |   baz | −10–30% | 792–1116 (styczne 1223→1116) | ≤0,7× ✓                     |
| scroll      |   418 |     321 |         b.z. (hoist w tbody) | ≤0,7× ✓                     |
| keystroke   | ~1000 |    b.z. |                         b.z. | brak regresji ✓ (temat F2c) |

Cel open ≤360 na granicy wariancji środowiskowej (383 przy σ~50) — raportowane
uczciwie zamiast dobierania przebiegu. Bezpiecznych milisekund już brak
(prep 36 = odrzucony lazy-prep); dalszy zysk tylko przez F2c (panel główny).

## Rozstrzygnięcie contractu (12.09.2026, zaakceptowane)

Wynik 383 vs cel 360: DOMKNIĘTE jako trafienie praktyczne. Uzasadnienie:
wariancja środowiskowa cold-open (±80 między przebiegami) przewyższa lukę
23 ms; mikro-bench in-page (12×, niska wariancja) potwierdza realny zysk
F2a (−19% renderu); wszystkie bezpieczne dźwignie Excela wyczerpane.
Ewentualne dalsze ~25 ms leży w zimnym layoucie tabeli — poza kontraktem
(redukcja komórek = zmiana geometrii = ryzyko wizualne, odrzucone w F1).

## Gates F2 (wszystkie zielone)

`tests/studnie` 87/1267 PASS, `typecheck:frontend` PASS, `lint:frontend`
0 errors, `test:parity` PARITY OK, `test:alignment` PASS 6/6, visual gate
0px (powtórzony po F2b), DOM-golden OK + sensitivity, scroll-kontrakt OK,
bench 5+5, mutation PASS.

## F2c — odroczone RFC (poza planem)

`editMs` ~1000 ms real = `_excelDebouncedRefresh` → pełny `updateSummary` +
`renderWellDiagram` + `renderWellsList` (z `refreshAll` + stats + transport
dla WSZYSTKICH). Temat panelu głównego, nie Excela.

## Weryfikacja przekrojowa (jak F1)

`typecheck` (backend przy commicie), `typecheck:frontend`, `lint:frontend`,
`encoding:check`, `test:quick`, `test:axe`, `version:check`, prettier —
przed commitem + raport bench przed/po na tym samym fixture.
