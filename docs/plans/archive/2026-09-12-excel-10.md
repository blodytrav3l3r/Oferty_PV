# Pakiet „Excel 10/10” — G1 + G2 + G3 (WYKONANY 12.09.2026)

Data: 2026-09-12
Status: WYKONANY (STOP-CONTROL, bez commita — osobna decyzja)
Poprzednik: `2026-09-12-excel-f2.md` (tam: open, scroll, MANUAL-commit)
Cel pakietu: ścieżka AUTO-commit (solver + rendery) + metryki + odporność
na serie szybkich edycji. Solver i konfigurator główny nietknięte.

## Krok 0. Audyt G3 (read-only, werdykt: chain, nie token)

- `autoSelectComponents` ma guard `isAutoSelectRunning` (odrzuca nakładkę)
  i commituje stan synchronicznie po `await` — starszy przebieg NIE MOŻE
  nadpisać nowszego (nowszy nigdy nie startuje równolegle).
- Prawdziwy defekt: odrzucony przebieg = mutacja rzędnych już w modelu,
  solver pominięty → config vs aktualne rzędne + ryzyko cap 10 przy serii.
- Wniosek: zamiast tokena na render — łańcuch re-runów (coalesce) we
  wrapperze Excel. Token na sam render byłby leczeniem objawu.

## G2. Split metryk (zero kodu prod)

- `excelOpenPerf.cjs`: `editMs` = AUTO (legacy seria, pierwszy wiersz
  fixture), nowe `editManualMs` = MANUAL (debounced-path). Historia nie
  kasowana.
- Baseline G2 (przed G1, cichy host): AUTO ~600 ms real, MANUAL ~114 ms real.
  Potwierdza: problem leży w AUTO, MANUAL po F2c zdrowy.

## G1. Quiet-render (ten sam stan, ~0 renderów w trakcie)

- `excelState.js`: `_excelQuietDepth` + INVARIANT (tylko rendery; nigdy wynik
  solvera, stan, telemetry, AI, błędy, kolejność) + bilans 0→1→0 w `finally`
  z resetem alarmowym.
- `solverAutoSelect.js` (3 punkty, wzór `__bulkQuiet`): LOADING bez
  `refreshAll`; błąd bez toasta/rendera (siedzi w `configErrors` → tint);
  finał bez 4 renderów. Telemetry pasywne bez zmian.
- Wrappery `excelAutoSelect.js` (auto-commit + run-przycisk): depth++/finally.
- Dowód A/B (stash): stan identyczny (config+status), rendery 4 (2×lista +
  2×refreshAll) → 0, zostaje 1× `_excelRenderTable`, depth wraca do 0.

## G3. Chain-coalesce (latest-wins przez re-run)

- `_excelAutoChain[wIdx]` `{running, queued}` w `_excelAutoSelectForWell`:
  nakładka kolejkuje jeden re-run na finalnych danych (sekwencyjne `await`,
  cap solvera nieosiągalny z łańcucha).
- Po drodze naprawiony własny błąd składni (zjedzona klamra inner-finally
  przy `do/while` w `try` — lekcja: liczyć klamry przed wysłaniem edita).
- Gate (wzmocniony): 5 rapidów → final == ostatni, depth 0, brak wyjątków;
  wolny #1 + szybki #2 → final == #2; wymuszone nakładanie (solver +300 ms)
  → 3 commity = 2 rendery, final == ostatni. PASS.
- Odstępstwo jawne: nie „dokładnie 1 render” (niemożliwe bez debounce
  solvera = opóźnienie feedbacku), tylko bounded + stan == ostatni.

## Wynik (ten sam fixture 1200, mediana 5+5, cichy host)

| Metryka              |     Przed |        Po | Cel             |
| -------------------- | --------: | --------: | --------------- |
| edit AUTO real       |      ~600 |       ~90 | ≤400 ✓✓         |
| edit MANUAL real     |      ~114 |      ~100 | b.z. ✓          |
| open cold / warm     | 602 / 513 | 530 / 468 | brak regresji ✓ |
| quietDepth po commit |         — |         0 | 0 ✓             |

## Gates (wszystkie zielone)

`tests/studnie` 87/1267, `typecheck:frontend`, `lint:frontend` 0 errors,
`test:parity`, `test:alignment` 6/6, visual 2× 0px, DOM + sensitivity,
scroll-kontrakt, G1-licznik, G3-race. Pełny `test:quick` + `test:axe`:
przed commitem (jak F2).

## Weryfikacja przekrojowa (jak F2)

Przed commitem: `test:quick`, `test:axe`, `typecheck`, `version:check`,
`encoding:check`, prettier, bench na tym samym fixture.
